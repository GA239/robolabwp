using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

using System.Windows.Resources;
using System.Windows;


namespace RoboLabWP.Model
{
    public class MZApplicationData
    {
        //MZAudio* _backgroundMusic;
        //MZAudio* _winSound;
        //public Texture2D _nanobotTexture;
        //public Texture2D _exitTexture;
        //public Texture2D _cellTexture;
        //public Texture2D _wallTexture;
        
        /*
        public MZApplicationData(Texture2D nanobotTexture, Texture2D exitTexture, Texture2D cellTexture, Texture2D wallTexture)
        {
            _nanobotTexture = nanobotTexture;
            _exitTexture = exitTexture;
            _cellTexture = cellTexture;
            _wallTexture = wallTexture;

            //string path = "Modules.MZ";            
            //Uri uri = new Uri(path, UriKind.Relative);
            //StreamResourceInfo sri = Application.GetResourceStream(uri);
            //byte[] mod = new byte[sri.Stream.Length];
            //sri.Stream.Read(mod, 0, mod.Length);

        }*/
    }
}